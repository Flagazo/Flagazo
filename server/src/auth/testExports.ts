/** Lo que los tests de OAuth usan de varios módulos, en un solo import. */
export { discordProvider, googleProvider, openState, sealState } from './oauth';
export { usernameBase as usernameBaseForTests } from './identities';
